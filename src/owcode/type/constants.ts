declare global {
  namespace Game {
    const isWaitingPlayers: boolean;
    const isInSetup: boolean;
  }
  namespace Constants {
    const True: boolean;
    const False: boolean;
  }
}
export const a = 0; 

// export declare 