变量
{
	全局:
		0: a
		1: b
		2: c

}

规则("init")
{
	事件
	{
		持续 - 全局;
	}
	动作
	{
		设置全局变量(a, 空数组);
		设置全局变量(b, 空数组);
		设置全局变量(c, 0.000);
	}
}

规则("test")
{
	事件
	{
		持续 - 全局;
	}
	条件
	{
		正在等待玩家 == 真;
	}
	动作
	{
		在索引处设置全局变量(a, 2, 3.000);
		在索引处设置全局变量(b, 1, 2.000);
		修改全局变量(c, 加, 1.000);
		在索引处修改全局变量(a, 2, 加, 1.000);
		修改全局变量(c, 加, 5.000);
		在索引处修改全局变量(a, 3, 加, 6.000);
		等待(数组中的值(全局变量(a), 数组中的值(全局变量(b), 1)));
	}
}
